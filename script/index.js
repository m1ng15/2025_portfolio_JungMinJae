const bnrSwiper = new Swiper('.bnr_swiper', {
    autoplay:{delay:0,},
    loop:true,
    speed: 5000,
    slidesPerView:4,
    spaceBetween:30,
})
const snsSwiper = new Swiper('.sns_swiper', {
    autoplay:{delay:0,},
    loop:true,
    speed: 7000,
    slidesPerView:4,
    spaceBetween:30,
})